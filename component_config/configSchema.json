{
  "type": "object",
  "title": "extractor configuration",
  "required": [
    "#api_token",
    "newest_files"
  ],
  "properties": {
    "#api_token": {
      "type": "string",
      "format": "password",
      "title": "Storage Token",
      "propertyOrder": 10
    },
    "newest_files": {
      "type": "boolean",
      "title": "Only newest files",
      "format": "checkbox",
      "description": "Process only newest version of each file (by name).",
      "propertyOrder": 20
    }
  }
}
